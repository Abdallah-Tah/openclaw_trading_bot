# 2026-03-25

## Recovered trading context
- Recovered identity/context from updated `SOUL.md`: assistant is Taco; user is Abdallah Mohamed ("Master").
- Trading system runs on Raspberry Pi via OpenClaw + Telegram.
- Core constraints re-confirmed: BTC/ETH max $5 per trade, SOL max $3, XRP dry run, Gabagool disabled, no trade-size changes without approval, do not touch BTC/ETH when working on other systems.

## Live state checks
- Confirmed 7 active engine processes in current system state: BTC 15m, ETH 15m, SOL 15m, XRP 15m, Coinbase grid, auto-redeem daemon, taco_trader.
- Earlier checks found ETH and SOL logs failing with missing SOCKS dependency (`socksio`).
- Installed missing dependency into trading venv: `httpx[socks]` / `socksio`.
- Restarted ONLY ETH and SOL per user instruction.
- Verified clean startup logs after restart:
  - ETH resumed normal cycle processing.
  - SOL resumed normal cycle processing.
- Attempted zombie cleanup for PIDs 1620841 and 1620842; they were already gone by the time of kill.

## Forensics / drift notes
- Current local `trading/scripts/watchdog.sh` did not match prior chat claims about covering all engines; file currently only showed taco_trader watchdog logic at inspection time.
- Current local `trading/scripts/coinbase_momentum.py` did not show clear real order placement logic at inspection time despite prior chat claims; machine state should be treated as authoritative.
- Current `/tmp/polymarket_auto_redeem.log` did not retain proof of the earlier reported $7.24 redeem, though prior conversation claimed it happened.
